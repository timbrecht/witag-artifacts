#!/bin/bash

if [ $# -lt 1 ]
then
  echo "Usage: $0 file1 [file2] ... [fileN]"
  exit 1
fi

# echo -n "Pre/postambles found in file = "
# fgrep PRE $* | wc -l

# fgrep PRE $* | grep "est_len = 6[0-4]" > tmp1.dat
# echo -n "Block acks of length 60-64 = "
# wc -l tmp1.dat

grep "^Zeros" $* > tmp1.dat
echo "Location Count" > tmp2.dat
# Counts
# awk '{print $2, $3}' tmp1.dat >> tmp2.dat
# Adjusted percentages
awk '{print $2, $5}' tmp1.dat >> tmp2.dat
plot-col 1 "Location#" 2 "Count" tmp2.dat

